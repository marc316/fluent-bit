#version: 4
#updates:
  # Maintain dependencies for GitHub Actions
  # - package-ecosystem: "github-actions"
  # directory: "/"
  # schedule:
  #   interval: "daily"
  # commit-message:
  #   prefix: "workflows: " 
